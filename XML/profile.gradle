profiler {
    // Set the number of warm-up iterations before profiling starts
    warmUps = 1

    // Set the number of measurement iterations
    iterations = 5

    // Specify the desired profiler(s) to use
    profilers {
        buildTime {}
    }
}
